# Copia este archivo a .env (que NO se sube al repo) para poder avisar a los
# jugadores tras actualizar los datos:  python3 import_data.py --avisar
SUPABASE_URL=https://TU-PROYECTO.supabase.co
SUPABASE_ANON_KEY=sb_publishable_xxx        # o la anon/public (eyJ...)
GPS_NOTIFY_SECRET=pon-aqui-un-valor-largo-y-secreto   # igual que el secret de la Edge Function

# Claves VAPID del proyecto (guárdalas aquí para no perderlas).
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
